# 2026-06-07

- 商家端套餐入口：在 `frontend/src/pages/MerchantConsole/components/ProductsTab.tsx` 为商品管理新增“新建套餐”按钮，复用现有 `BundleGroupsEditor`，打开时默认启用套餐类别；创建弹窗根据普通菜品/套餐切换标题、说明、名称占位和提交按钮，并补充新建/编辑套餐空配置校验与提示，防止空套餐被静默保存为普通商品。验证：`npm run typecheck --prefix frontend` 通过，`cd backend && sbt -batch compile` 通过；类型安全审计脚本仍报告既有 order chat API 拆分相关失败，非本次改动引入。
- 商家端优惠优化：修改 `frontend/src/components/PromotionControls.tsx`，日期选择改为原生日期输入（支持日历和数字输入），启用控件改为启用/停用下拉；修改 `frontend/src/pages/MerchantConsole/components/ProductsTab.tsx`，店铺优惠移除“无条件”触发选项并停用置灰，单菜品优惠改为显示当前菜品/原价、输入现价、展示折扣，提交时校验现价必须低于原价。验证：`npm run typecheck --prefix frontend` 通过。
- 商家端菜品/套餐本地图片上传与套餐创建修复：`use-merchant-console-store.ts` 的 `createProduct` 改为返回创建出的 `Product`；`MerchantConsole/index.tsx` 将创建回调继续返回 Product；`ProductsTab.tsx` 在新建菜品/套餐弹窗加入本地图片选择，创建成功后用返回的 `productId` 调用已有 `onUploadProductImage` 上传图片；套餐创建允许描述为空并用“精选套餐”兜底，修复因描述校验导致的套餐创建阻塞。验证：`npm run typecheck --prefix frontend` 通过。
- 全局/平台优惠设置方式调整：`AdminConsole/index.tsx` 首页平台优惠改为只读单行摘要，停用优惠灰色展示，并提供“管理优惠”入口；新增 `AdminConsole/PlatformPromotionsPage.tsx` 和 `/delivery/admin/promotions` 路由，支持管理/新增优惠、右上角新增和提交按钮、草稿修改仅提交后保存、未提交退出提示（左上返回优惠管理、不保存并退出、保存并退出）。验证：`npm run typecheck --prefix frontend` 通过。
- 套餐契约与创建错误修复：修复 `bundleGroups[0].options[0].recommended missing required field`，在前端套餐选项补齐 `recommended/extraPrice`，后端 `ApiJsonCodecs.scala` 对 `ProductBundleOption/ProductBundleGroup` 增加兼容缺省字段 decoder；套餐模型扩展为自定义套餐价 + 类别包含价 + 选项自定义加价，类别支持 `fixed/repeatable/nonRepeatable`，创建/编辑/结算/顾客端选择均适配；后端和前端允许菜品、套餐描述为空。验证：`npm run typecheck --prefix frontend` 通过，`cd backend && sbt -batch compile` 通过。
- 商家/管理员优惠管理交互统一：新增 `frontend/src/components/PromotionEditorCard.tsx` 复用优惠编辑表单并去掉描述末尾“· 优惠”；`ProductsTab.tsx` 中商家店铺优惠首页改为只读单行、每条提供“管理优惠”、右上角新增优惠，编辑/新增在弹窗中草稿修改并提交保存，未提交退出提示含“返回优惠管理/不保存并退出/保存并退出”，管理弹窗内禁用不变灰；管理员首页每条平台优惠也增加管理按钮并复用相同摘要文案，管理员管理页复用同一编辑卡片且禁用不变灰。验证：`npm run typecheck --prefix frontend` 通过。
- 商家端套餐菜品选择体验优化：`ProductsTab.tsx` 的 `BundleGroupsEditor` 改为点击“添加类别”后打开菜品选择页，确认后才在套餐类别中显示菜品；已添加类别可通过“添加/编辑菜品”再次进入选择页，也可删除单个菜品；套餐菜品卡片内展示菜品名、原价、加价并在卡片内编辑加价，不再额外新增加价卡片；包含价无自定义加价时默认显示所选菜品最高价，有自定义加价时显示为空。`ProductBundleOption` 新增 `customExtraPrice` 并在后端 codec 兼容持久化。验证：`npm run typecheck --prefix frontend` 通过，`cd backend && sbt -batch compile` 通过。
- 优惠界面细节修复：`ProductsTab.tsx` 商家店铺优惠管理弹窗设置 `showCloseButton={false}`，删除与返回按钮重复的右上叉号；`PromotionEditorCard.tsx` 新增默认优惠排期工具，新建商家/管理员优惠默认开始日期为今日、结束日期为下个月同日，时段为 `00:00-23:59`；单菜品优惠新增时也复用该默认排期。验证：`npm run typecheck --prefix frontend` 通过。
- 套餐类别布局与包含价输入修复：`ProductsTab.tsx` 中套餐“类别名称”独占更宽第一行，类别类型/件数/包含价移至下一行；包含价在自定义加价后显示为空但仍可输入，输入后会清空该类别所有菜品的自定义加价并用新的包含价替换加价模式。验证：`npm run typecheck --prefix frontend` 通过。
- 套餐与优惠计价修复：`frontend/src/lib/bundles.ts` 和后端 `OrderAPIMessageSupport.scala` 统一为套餐基础价 + 显式加价/包含价差价，避免把菜品原价当加价；`ProductsTab.tsx` 保留商家输入的套餐包含价并让菜品优惠提交/删除立即持久化；用户商家页、套餐弹窗、结算页显示优惠原价划线/现价/几折；`promotions.ts` 与 `PromotionPricing.scala` 让单菜品优惠按购买数量抵扣并参与结算。验证：`npm run typecheck --prefix frontend` 通过，`cd backend && sbt -batch compile` 通过。
- 套餐基础价保存补修：`ProductsTab.tsx` 将新建/编辑套餐的基础价输入、套餐开关、套餐类别 onChange 改为函数式状态更新，避免类别编辑用旧表单状态覆盖 `price`；商品列表和新建弹窗文案改为“套餐基础价”；后端创建/更新商品 API 将原 `bundleBasePrice` 逻辑改为只校验套餐配置的 `validateBundleGroups`，明确保存请求中的 `price`。验证：`npm run typecheck --prefix frontend` 通过，`cd backend && sbt -batch compile` 通过。
- 顾客端通知与自动消息模板：`GlobalNotificationCenter.tsx` 将顾客端通知入口改为右上角铃铛按钮，未读时显示红点，点击展示最近通知并支持全部已读/点击跳转；新增 `OrderChatNotificationTemplates.scala`，商家出餐 `MerchantOrderReadyAPIMessage.scala` 自动向顾客聊天发送出餐通知，骑手送达 `RiderUpdateOrderStatusAPIMessage.scala` 自动向顾客聊天发送送达通知。验证：`npm run typecheck --prefix frontend` 通过，`cd backend && sbt -batch compile` 通过。
- 清理残留上传数据：按用户要求删除 `backend/refund-uploads/` 与 `backend/review-uploads/` 两个数据目录，并确认目录已不存在。
- 类型安全与结构审计：运行 `type-safety-audit` 脚本发现并修复唯一问题：前端残留聚合 `frontend/src/apis/order/OrderChatAPI.ts`（一个文件定义 12 个 API class，且无后端同名对应）；已将引用改为 `OrderChatClient.ts` 并删除聚合文件。复验审计 `42 通过, 0 失败`，`npm run typecheck --prefix frontend` 与 `cd backend && sbt -batch compile` 均通过。
- 商家端联系页返回优化：`MerchantConsole/index.tsx` 不再每次挂载都 `resetPage`，改为读取 `useAuthSession` 并调用 `prepareForSession`；`use-merchant-console-store.ts` 新增 `sessionAccount` 与按账号变化重置逻辑，保留同账号从订单聊天页返回时的店铺选择与弹窗关闭状态，避免返回后跳到选择店铺页面。验证：`npm run typecheck --prefix frontend` 通过。
- 商家优惠排期校验与提示层级优化：`PromotionEditorCard.tsx` 新增统一排期校验，要求开始日期不早于今日、截止日期晚于开始日期、每日开始/截止时间不相同，截止早于开始时展示“次日截止”语义；`ProductsTab.tsx` 在店铺优惠和菜品优惠提交前调用校验并提示商家；`PromotionControls.tsx` 为日期输入加 `min`；`AppChromeProvider.tsx` 将全局提示提升到 `z-[9999]`；前后端优惠生效逻辑均将相同开始/截止时间视为无效。验证：`npm run typecheck --prefix frontend` 与 `cd backend && sbt -batch compile` 通过。
- 优惠日期输入按钮精简：`PromotionControls.tsx` 删除额外自定义日历图标按钮，保留原生 `type="date"` 日期输入及“清空”按钮，避免同一日期字段出现两个日期选择入口。验证：`npm run typecheck --prefix frontend` 通过。
- 订单价格快照 + 结算明细解释器：新增后端 `OrderPriceSnapshot/OrderPriceBreakdown` 对象、订单表 `price_snapshot/price_breakdown` JSONB 列、下单构建时保存每笔订单商品快照/商家优惠/优惠券/平台优惠/配送费/实付明细，并在 `CheckoutResponse` 返回汇总结算明细；前端新增对应对象和 `lib/order-price-breakdown.ts` 解释器，结算页金额区与顾客订单详情统一按结构化明细渲染，历史订单可用旧金额字段兜底。验证：`npm run typecheck --prefix frontend` 与 `cd backend && sbt -batch compile` 通过。
- 通知已读状态后端化：新增 `notification_read_states` 表与 `NotificationReadStates/NotificationMarkRead/NotificationMarkAllRead` API，扩展 API 注册支持多角色访问；前端 `GlobalNotificationCenter.tsx` 拉取后端已读 ID，过滤已读通知，点击通知或“全部已读”时写回后端，同时保留本地通知列表作展示缓存。验证：`npm run typecheck --prefix frontend` 与 `cd backend && sbt -batch compile` 通过。
- 订单状态机集中化：新增 `OrderStatusTransitionService.scala`，集中定义订单状态流转矩阵、统一校验、保存订单和出餐/送达系统聊天通知副作用；商家接单/拒单/出餐、骑手抢单/送达、顾客取消/确认完成、退款通过均改为调用状态机，移除各 API 内分散的 `order.copy(status = ...)` 与重复通知创建。验证：`npm run typecheck --prefix frontend` 与 `cd backend && sbt -batch compile` 通过。
- 菜品库存和售罄：`Product` 前后端新增 `inventoryMode`（`unlimited/finite/soldOut`）与 `maxPerOrder`，商品表新增 `inventory_mode/max_per_order`；商家端商品创建/编辑支持无限库存、今日库存、售罄、每单限购；顾客端商家页和套餐选择弹窗展示剩余库存/售罄/限购并禁用不可购买操作；后端 `Checkout` 校验上下架、售罄、库存不足、每单限购并在结算成功后扣减有限库存。验证：`npm run typecheck --prefix frontend` 与 `cd backend && sbt -batch compile` 通过。
- 商家营业状态和休息时间：新增 `MerchantBusinessHours` 前后端对象与 `MerchantBusinessHoursAPI`，商家表/目录表持久化 `business_status/weekly_business_hours/holiday_business_hours`；商家“我的”页可设置营业中/休息中/今日打烊/临时暂停接单、每周营业时间和节假日特殊营业时间；顾客进店页展示“营业中，可下单”或“休息中，不可下单”及预计开始营业时间，非营业时禁用加入/结算；后端结算通过 `MerchantBusinessHoursSupport` 校验店铺可接单。验证：`npm run typecheck --prefix frontend` 与 `cd backend && sbt -batch compile` 通过。
- 菜品库存/售罄/限购补齐检查：修复 `CatalogProductTable.list` 漏查 `inventory_mode/max_per_order`，结算改用 `listForUpdate` 锁定商品行；后端商品 DTO 同步库存字段，`Checkout` 增加商品存在、商家归属、数量合法性校验；前端新增 `lib/cart-inventory.ts` 统一购物车库存/限购计算，刷新购物车、再来一单、普通菜品/套餐加购、购物车加号和套餐选择均按库存/售罄/限购裁剪或禁用。验证：`npm run typecheck --prefix frontend`、`cd backend && sbt -batch compile`、类型安全审计脚本均通过。
- 管理员订单监控：新增 `AdminOrderMonitorAPIMessage` / `AdminOrderMonitorAPI.ts` 和 `AdminOrderMonitorResponse` 前后端契约，后台首页展示今日订单数、今日成交额、待处理退款、异常订单、商家超时订单、骑手超时订单；后端基于 `OrderTable.list`、`RiderAssignmentTable.listAll`、退款状态、订单状态和超时阈值计算监控列表，前端 `AdminConsole` 增加监控指标卡和异常/超时/退款列表。验证：`npm run typecheck --prefix frontend`、`cd backend && sbt -batch compile`、类型安全审计脚本均通过。
- 订单实时状态时间线与备餐动态调整：`Order` 新增 `OrderTimelineEvent/statusTimeline/estimatedPrepMinutes/estimatedReadyAt/prepDelayReason/prepDelayedAt/prepTimeoutNotifiedAt` 并落库；状态机统一追加已下单、商家接单、出餐、骑手接单/取餐/配送、送达、完成等时间线节点；商家接单可选 10/15/20/30/自定义备餐时间，新增 `MerchantOrderPrepDelayAPI` 支持主动延迟备餐并通知顾客；顾客订单详情展示时间线、预计出餐、25 分钟范围预计送达和异常提示，我的页待收货订单展示简版时间条。验证：`npm run typecheck --prefix frontend`、`cd backend && sbt -batch compile`、类型安全审计脚本均通过。
- 类型安全复查：按要求运行类型安全审计、前端 `npm run typecheck --prefix frontend`、后端 `cd backend && sbt -batch compile`，结果均通过；审计为 `42 通过, 0 失败`。
- 结构优化建议梳理：基于当前文件规模和代码结构，识别出前端 `ProductsTab/ProfileTab/CustomerMerchantOrderPage/use-customer-portal-store/AdminConsole/GlobalNotificationCenter` 等大文件，以及后端 `ApiJsonCodecs/OrderTable/OrderAPIMessageSupport` 可作为后续拆分和领域服务化重点。
- 类型安全 skill Unsafe 检查更新：`type-safety-audit` 的 `SKILL.md`、问题目录和 `check-type-safety.sh` 新增 Unsafe/Unsfe 组件、`dangerouslySetInnerHTML/eval/new Function/as any/ts-ignore`、后端 `unsafeRun/unsafeFromString/asInstanceOf` 检查；修复程序中 `OpenAIClient.scala` 的 `Uri.unsafeFromString`，改为 `Uri.fromString` + `IO.fromEither`。验证：更新后审计 `43 通过, 0 失败`，前端 typecheck 和后端 compile 均通过。
- 后端运行问题排查：检查 8787/5173 端口和 sbt/java 进程，发现无初始残留；系统 `/usr/bin/java` 无法定位 JDK，但 Homebrew `openjdk` 可用。已更新根 `package.json` 的 `dev:backend` 脚本，在未设置 `JAVA_HOME` 时自动使用 `/opt/homebrew/opt/openjdk@21` 或 `/opt/homebrew/opt/openjdk` 并加入 PATH；清理验证过程中产生的 8787 Java 进程，确认端口释放。提示：JDK 25 下 `sun.misc.Unsafe` 与 IOApp 线程警告来自 Scala/sbt/JDK 组合，非项目 Unsafe 代码。
- 前端等待卡住修复：`npm run dev` 卡在 `dev:frontend:wait` 的原因是 `/api/catalogapi` 返回 500：`catalog_merchants.business_status` 缺失。已修复 `MerchantStoreTableInitializer.scala`，为既有 `merchant_stores/catalog_merchants` 表补齐 `business_status/weekly_business_hours/holiday_business_hours` 的 `ALTER TABLE ADD COLUMN IF NOT EXISTS`；并对当前 Docker PostgreSQL 立即执行迁移。验证：`catalogapi_ok`，Vite 已监听 `5173`，后端 compile 通过。
- OpenJDK 21 配置：通过 Homebrew 安装 `openjdk@21 21.0.11`；更新根 `package.json` 的 `dev:backend`，优先设置 `JAVA_HOME=/opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home` 并加入 PATH，回退到普通 `openjdk`。验证脚本逻辑输出 `openjdk version "21.0.11"`，后端在 `Homebrew Java 21.0.11` 下 `sbt -batch compile` 通过；已停止仍使用 JDK 25 的旧 dev 进程，需重新运行 `npm run dev` 生效。
