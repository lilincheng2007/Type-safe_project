# 2026-06-08

- 文档更新：按当前代码结构更新三个 README：根目录 `README.md`、`frontend/README.md`、`backend/README.md`。补充四角色功能、管理员/评价/聊天/通知已读/库存/营业时间/订单时间线/价格快照/平台优惠等能力，更新 API 清单、路由、静态图片路径、演示账号、启动命令、目录结构和开发约定；执行 `git diff --check -- README.md frontend/README.md backend/README.md` 通过。
- 文档清理：按用户要求删除根目录多余副本 `README 2.md`。
- 文档清理：继续删除 `frontend/README 2.md` 与 `backend/README 2.md` 两个多余 README 副本。
- Git 忽略规则：确认 `backend/.codex-tmp/` 是 Codex/sbt 运行留下的 coursier、ivy2、sbt 依赖缓存目录，已加入根 `.gitignore`；`git check-ignore` 验证规则生效。
- 数据库演示数据：按用户要求直接写入本地 PostgreSQL `delivery_backend`，为 `merchant_demo` 幂等新增 `demo-extra-store-01` 到 `demo-extra-store-10` 共 10 家店，并在 `catalog_products` 写入 100 个菜品；校验 `merchant_stores=10`、`catalog_merchants=10`、`catalog_products=100`、每店 10 个菜品、无 orphan 记录；临时 SQL 文件已删除。
- 商家端标签选择：为 `MerchantConsole` 新店铺入驻申请新增多选标签（中餐、西餐、饮品甜点、其它），前端 `CreateStoreRequest`/`MerchantStoreAPI` 携带 `tags`；后端入驻申请表新增 `tags` JSONB 并在审核通过时写入正式店铺，`MerchantCategory` 增加“其它”；验证 `npm run typecheck`、本次前端相关文件 `eslint`、`sbt -batch compile` 通过。
- 用户端支付修复：定位支付/结算报错 `The column index is out of range: 37, number of columns: 36` 到 `backend/src/order/tables/order/OrderTable.scala` 的订单插入 SQL，因 `prep_timeout_notified_at` 缺少 VALUES 占位符导致第 37 个参数绑定越界；已在 `now()` 前补充第 37 个 `?`，校验占位符数与最大绑定索引均为 37，`sbt -batch compile` 通过。
- AI 饮食周报优化：按用户要求增强 `AIDietWeeklyReportAPIMessage`，提示词要求输出个性化 `dietAnalysis` 和 4-6 条可执行饮食建议；前后端 `AIDietWeeklyReportResponse` 新增 `dietAnalysis`，顾客端 `ProfileTab` 在周报卡片展示“顾客饮食分析”和“饮食建议”；验证 `npm run typecheck`、相关文件 `eslint`、`sbt -batch compile` 通过。
- 用户端订单详情弹窗：按用户反馈将 `OrderDetailDialog` 改为固定最大高度 `90vh/46rem`，头部与底部操作栏固定，中间订单详情内容使用 `overflow-y-auto` 滚动，解决详情卡片过大问题；验证该文件 ESLint 与 `npm run typecheck` 通过。
- 卡片化可读性优化：按用户反馈将顾客新增收货地址弹窗 `DeliveryContactAddDialog` 改为卡片样式（背景、边框、阴影、白底输入框、错误卡片），将商家端退款申请条目与退款驳回/同意弹窗 `MerchantReviewsTab` 改为卡片样式（待处理高亮、白底输入区）；验证 `npm run typecheck`、`DeliveryContactAddDialog` ESLint、`git diff --check` 通过，`MerchantReviewsTab` 仍有既有 hooks/unused-vars lint 问题未处理。
