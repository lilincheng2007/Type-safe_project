# 2026-06-14

- 完成 `OPTIMIZATION_PLAN.md` 第五批优化：新增并接入后端枚举 `MerchantBusinessStatus`、`ProductInventoryMode`、`ProductBundleSelectionType`、`PromotionDiscountType`、`PromotionTriggerType`、`OrderChatRole`、`OrderChatMessageType`，更新领域对象、业务逻辑、表读写和 JSON codec；前端补齐同名类型文件并在现有类型中复用。同步更新优化计划复盘和第六批 JSON codec 模块化建议；验证后端编译、前端 typecheck、类型安全审计、可维护性审计均通过。
- 完成 `OPTIMIZATION_PLAN.md` 第六批优化：新增 `platform/json/CommonJsonCodecs.scala`，将业务 codec 下沉到各模块 `json/*JsonCodecs.scala`，`ApiJsonCodecs.scala` 收敛为聚合导出；同步更新 `DIRECTORY_LAYERING_GUIDE.md`、`AGENTS.md`、`backend/README.md`、优化计划复盘和第七批建议。验证后端编译、前端 typecheck、类型安全审计、可维护性审计均通过。
- 完成 `OPTIMIZATION_PLAN.md` 第七批优化：删除未使用 `merchantprofileapi`（前后端 API 文件与路由/清单同步移除）；新增语义化 `merchantcreatestoreonboardingrequestapi` 并保留 `merchantstoreapi` 兼容包装；将后端 `Promotion`/`Voucher`/`ErrorBody` 从 `delivery.domain.CompatibilityAliases` 切换为真实归属导入；同步更新 `API_INVENTORY.md`、`backend/README.md`、`frontend/README.md` 与计划复盘/第8批建议。验证后端编译、前端 typecheck、类型安全审计、可维护性审计均通过。
- 完成 `OPTIMIZATION_PLAN.md` 第八批优化：`OrderChatPage` 拆分为页面装配入口 + `useOrderChatMessages`/`useOrderChatPeerContext`/`usePendingChatImage` hooks 与 `ChatHeader`/`MessageList`/`ChatComposer` 组件；`AdminConsole` 拆分为 `useAdminConsoleData`、`useRefundReviewDialog` 和指标卡/入驻列表/退款弹窗组件；`CustomerPortal` 大 store 拆分为 `portalDataActions`、`cartActions`、`orderReviewActions`、`checkoutActions`、`profileAiActions` 子域 action 组合。同步更新第8批复盘与第9批建议（通知和结算预估后端化）；验证前端 typecheck、后端编译、类型安全审计、可维护性审计均通过。
