# 2026-06-14

- 完成 `OPTIMIZATION_PLAN.md` 第五批优化：新增并接入后端枚举 `MerchantBusinessStatus`、`ProductInventoryMode`、`ProductBundleSelectionType`、`PromotionDiscountType`、`PromotionTriggerType`、`OrderChatRole`、`OrderChatMessageType`，更新领域对象、业务逻辑、表读写和 JSON codec；前端补齐同名类型文件并在现有类型中复用。同步更新优化计划复盘和第六批 JSON codec 模块化建议；验证后端编译、前端 typecheck、类型安全审计、可维护性审计均通过。
- 完成 `OPTIMIZATION_PLAN.md` 第六批优化：新增 `platform/json/CommonJsonCodecs.scala`，将业务 codec 下沉到各模块 `json/*JsonCodecs.scala`，`ApiJsonCodecs.scala` 收敛为聚合导出；同步更新 `DIRECTORY_LAYERING_GUIDE.md`、`AGENTS.md`、`backend/README.md`、优化计划复盘和第七批建议。验证后端编译、前端 typecheck、类型安全审计、可维护性审计均通过。
