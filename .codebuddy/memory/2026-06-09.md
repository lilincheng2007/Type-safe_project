# 2026-06-09

- README 总结改版：按用户要求将原根目录 `README.md` 改名为 `README.full.md`，新建总览型 `README.md`（227 行），控制在不超过 9 页的 Markdown 总结范围内，覆盖类型与系统架构、主要功能、特点与亮点、快速启动、环境变量和参考文档；执行 `git diff --check -- README.md README.full.md` 通过。
- 项目规则重写：根据当前 `README.md`、`API_INVENTORY.md`、现有架构和功能边界重写根目录 `AGENTS.md`（177 行），覆盖项目定位、技术栈、启动方式、目录结构、APIMessage 类型安全规则、前端/后端开发规则、功能边界、验证命令和文档维护；执行 `git diff --check -- AGENTS.md` 通过。
- API 清单更新：根据当前后端 routes 注册和前端 `src/apis/**` 文件重写 `API_INVENTORY.md`（179 行），补齐 `admin`、`review`、订单聊天/通知/图片、商家退款/营业/促销等 API；当前统计 7 个模块、78 个 API，并列出角色、前后端文件和响应类型；执行 `git diff --check -- API_INVENTORY.md` 与类型安全审计脚本，结果 43 PASS / 0 FAIL。
- 六份项目文档一致性检查：按用户要求核对并修正 `AGENTS.md`、`API_INVENTORY.md`、`README.md`、`frontend/README.md`、`backend/README.md`、`README.full.md`，补齐 AI 评价摘要、骑手评分/评价、全局壳层与刷新策略、静态订单图片路由说明、`DB_POOL_NAME`/`SERVICE_INTERNAL_TOKEN`、`sample/` 与 `.codebuddy/skills/` 目录等内容；清理“取消退款”“追评”等不准确措辞；验证六份文档 `git diff --check`、read_lints 与类型安全审计脚本均通过。
