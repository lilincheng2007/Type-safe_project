# 2026-06-13

- 创建项目级 Skills：`.codebuddy/skills/maintainability-audit/`（含 `scripts/check-maintainability.sh` 与问题目录）和 `.codebuddy/skills/layered-feature-development/`（含分层落位决策树），用于保障 `Type-safe_project` 的长期可维护性。
- 新增 `DIRECTORY_LAYERING_GUIDE.md`，定义后端 `api/services/validators/tables/objects/json/shared/media` 与前端 `apis/objects/pages/stores` 的职责边界、API 契约流程和渐进迁移策略；同步更新 `AGENTS.md` 的结构、规则、验证命令和文档维护清单。
- 执行可维护性整改计划：将后端 `shared` Scala 实现拆分到 `platform/auth/db/bootstrap/domain/media/promotion`，迁移 `StoredImage` 到 `media`，将 `Promotion`/`Voucher` 归入 `promotion`，移除 `*APIMessageSupport.scala`，页面私有 Zustand store 迁入 `pages/{Page}/stores/`，并更新审计脚本与文档；最终后端编译、前端类型检查、类型安全审计、可维护性审计均通过。
- 删除已空的 `backend/src/shared` 目录树，并复跑可维护性审计通过（10 pass / 0 warn / 0 fail）。
- 按用户要求在根目录创建 `OPTIMIZATION_PLAN.md`，沉淀长期可维护性优化总目标、十批次整改计划、当前进展记录和代码现状快照；本次仅新增文档，未修改业务代码。
- 完成 `OPTIMIZATION_PLAN.md` 第一批优化：将错位的后端状态流转、退款流程、时间线、聊天通知模板、商家营业时间、管理员订单监控支撑逻辑从 `api/` 迁到 `services/` / `validators/`，同步更新引用、`backend/README.md` 与计划维护记录；验证 `sbt -batch compile`、类型安全审计、可维护性审计均通过。
- 完成 `OPTIMIZATION_PLAN.md` 第二批优化：删除后端源码中的 `*Support.scala` 泛化文件，将 Me 响应装配、商家店铺归属校验、自有商品列表、标准平台券和 JWT 能力拆到明确的 assembler/service/validator；同步更新 `backend/README.md` 与优化计划维护记录，并验证后端编译、类型安全审计、可维护性审计均通过。
